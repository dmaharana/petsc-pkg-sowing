/*@
  oldstyle - Shows all fancy formatting

  Input Parameters:
. x, y - Coords
. a - the `really` important value

  Output Parameters:
. p1 - pointer `to a`. Is this here?

  Notes:
  This code uses the old style for multiple arguments - just using multiple
  ''. '' commands.
@*/
void oldstyle(double x, double y, double a, int *p)
{
}

