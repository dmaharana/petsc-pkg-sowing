ALL: tohtml

DIRS = sys bfort tohtml doctext textfilt mapnames

clean:
	for dir in $(DIRS) ; do ( cd $$dir ; $(MAKE) clean ) ; done

manpages:
	for dir in $(DIRS) ; do ( cd $$dir ; $(MAKE) manpages ) ; done

wwwpages:
	for dir in $(DIRS) ; do ( cd $$dir ; $(MAKE) wwwpages ) ; done

